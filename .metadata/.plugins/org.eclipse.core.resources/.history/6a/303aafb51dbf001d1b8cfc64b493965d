spring:
   datasource:
    url: jdbc:mysql://DB HOST 주소?characterEncoding=UTF-8&serverTimezone=UTC
    username: DB ID
    password: DB PASSWORD
    driver-class-name: com.mysql.cj.jdbc.Driver
    
session:
    jdbc:
      initialize-schema: always
